version: 1
runtime: python
entry: src/server/server.py

build:
    install: uv sync --frozen --no-dev

startCommand:
    type: http
    command: .venv/bin/python -m server.server

configSchema:
    source: code

name: weavscope-docs
description: MCP documentation server for WeavScope - multi-tenant Weaviate wrapper package
