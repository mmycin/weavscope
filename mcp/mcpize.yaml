version: 1
runtime: python
entry: src/server/server.py

build:
    install: uv sync --frozen --no-dev

startCommand:
    type: http
    command: .venv/bin/python -m server.server

configSchema:
    source: code
